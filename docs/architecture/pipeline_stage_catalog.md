# Pipeline Stage Catalog

Each non-aggregate pipeline stage is listed with the library layers it coordinates, the wrapper module, and the generated detail view id.

| Order | Stage | Module | Wrapper Purpose | Library Layers | Layer Summary | LOC | Functions | Classes | Detail View |
| ---: | --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 1 | 00 Ingest Raw | `pipelines.00_ingest_raw` | Ingest raw parquet telemetry into normalized Delta bronze/silver tables. | Perf, Io, Config | Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Config: Modules grouped under libs.config | 106 | 2 | 0 | `pipeline_stage_00_ingest_raw` |
| 2 | 10 Parameter Profiles Fit | `pipelines.10_parameter_profiles_fit` | Fit datatype and scaling profile artifacts from raw telemetry. | Io, Profiling, Perf, Behavior, Config, Pyspark, Common | Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Profiling: libs/profiling owns parameter profiling over canonical telemetry: datatype profiling; continuous scaling/profile statistics; behavior-family profiling; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Behavior: libs/behavior owns the parameter behavior families used by simulation | 134 | 2 | 0 | `pipeline_stage_10_parameter_profiles_fit` |
| 3 | 12 Behavior Profiles Fit | `pipelines.12_behavior_profiles_fit` | Fit behavior primitive and family profile artifacts from raw telemetry. | Io, Profiling, Perf, Behavior, Config, Pyspark, Common | Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Profiling: libs/profiling owns parameter profiling over canonical telemetry: datatype profiling; continuous scaling/profile statistics; behavior-family profiling; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Behavior: libs/behavior owns the parameter behavior families used by simulation | 187 | 2 | 0 | `pipeline_stage_12_behavior_profiles_fit` |
| 4 | 15 Event Profiles Fit | `pipelines.15_event_profiles_fit` | Fit parameter-level event detector policy profiles from raw telemetry. | Events, Io, Perf, Windows, Profiling, Behavior, Config, Spark Sequence, Pyspark, Common | Events: libs/events owns canonical event detection and event validation; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Windows: libs/windows owns: window lifecycle and closure semantics; window-policy profiling and selection; per-window signal buffering; the canonical segmented Spark window builder | 142 | 2 | 0 | `pipeline_stage_15_event_profiles_fit` |
| 5 | 20 Events Extract | `pipelines.20_events_extract` | Extract event stream from mixed-rate sensor channels. | Events, Io, Perf, Windows, Profiling, Behavior, Config, Spark Sequence, Pyspark, Common | Events: libs/events owns canonical event detection and event validation; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Windows: libs/windows owns: window lifecycle and closure semantics; window-policy profiling and selection; per-window signal buffering; the canonical segmented Spark window builder | 184 | 1 | 0 | `pipeline_stage_20_events_extract` |
| 6 | 25 Window Policy Profile | `pipelines.25_window_policy_profile` | Fit a data-driven window policy profile from detected events. | Windows, Io, Perf, Config, Spark Sequence, Pyspark, Common | Windows: libs/windows owns: window lifecycle and closure semantics; window-policy profiling and selection; per-window signal buffering; the canonical segmented Spark window builder; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Config: Modules grouped under libs.config | 153 | 1 | 0 | `pipeline_stage_25_window_policy_profile` |
| 7 | 30 Windows Adaptive | `pipelines.30_windows_adaptive` | Build adaptive windows from event thresholds and max duration. | Windows, Io, Perf, Config, Spark Sequence, Pyspark, Common | Windows: libs/windows owns: window lifecycle and closure semantics; window-policy profiling and selection; per-window signal buffering; the canonical segmented Spark window builder; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Config: Modules grouped under libs.config | 160 | 1 | 0 | `pipeline_stage_30_windows_adaptive` |
| 8 | 40 Backbone Fit | `pipelines.40_backbone_fit` | Fit backbone artifacts from adaptive windows and raw telemetry. | Windows, Io, Perf, Backbone, Config, Spark Sequence, Pyspark, Common | Windows: libs/windows owns: window lifecycle and closure semantics; window-policy profiling and selection; per-window signal buffering; the canonical segmented Spark window builder; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Backbone: libs/backbone owns the continuous reconstruction backbone used to summarize normal multivariate structure and sensor importance | 282 | 1 | 0 | `pipeline_stage_40_backbone_fit` |
| 9 | 50 Build Graph | `pipelines.50_build_graph` | Build graph component artifacts from backbone, events, and windows. | Graph, Io, Perf, Config, Pyspark | Graph: libs/graph owns graph-domain models built from telemetry windows and events; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Config: Modules grouped under libs.config | 547 | 7 | 0 | `pipeline_stage_50_build_graph` |
| 10 | 60 Fit Hierarchy | `pipelines.60_fit_hierarchy` | Fit hierarchy artifacts from fused graph and the persisted graph parameter universe. | Graph, Io, Perf, Config, Pyspark | Graph: libs/graph owns graph-domain models built from telemetry windows and events; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Config: Modules grouped under libs.config | 141 | 2 | 0 | `pipeline_stage_60_fit_hierarchy` |
| 11 | 70 Phase Fit | `pipelines.70_phase_fit` | Fit phase baselines and assign detected phases to windows. | Phase, Perf, Io, Backbone, Config, Spark Sequence, Pyspark, Common | Phase: libs/phase owns phase feature selection, phase detection, phase analysis, and phase validation; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Backbone: libs/backbone owns the continuous reconstruction backbone used to summarize normal multivariate structure and sensor importance | 209 | 2 | 0 | `pipeline_stage_70_phase_fit` |
| 12 | 72 Phase Label Centroids | `pipelines.72_phase_label_centroids` | Build validation-only centroids from truth-labeled phase windows. | Phase, Perf, Io, Backbone, Config, Spark Sequence, Pyspark, Common | Phase: libs/phase owns phase feature selection, phase detection, phase analysis, and phase validation; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Backbone: libs/backbone owns the continuous reconstruction backbone used to summarize normal multivariate structure and sensor importance | 141 | 1 | 0 | `pipeline_stage_72_phase_label_centroids` |
| 13 | 80 Window Scores Raw | `pipelines.80_window_scores_raw` | Build raw window scores from phase windows and phase baselines. | Phase, Graph, Io, Perf, Scoring, Backbone, Config, Spark Sequence, Pyspark, Common | Phase: libs/phase owns phase feature selection, phase detection, phase analysis, and phase validation; Graph: libs/graph owns graph-domain models built from telemetry windows and events; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation | 118 | 1 | 0 | `pipeline_stage_80_window_scores_raw` |
| 14 | 85 Window Scores Calibrate | `pipelines.85_window_scores_calibrate` | Calibrate raw window scores with phase-conditioned conformal calibration. | Io, Perf, Scoring, Graph, Phase, Backbone, Config, Spark Sequence, Pyspark, Common | Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Scoring: libs/scoring owns raw and calibrated anomaly scoring over fitted phase and structural artifacts; Graph: libs/graph owns graph-domain models built from telemetry windows and events | 93 | 1 | 0 | `pipeline_stage_85_window_scores_calibrate` |
| 15 | 90 Anomaly Attribution | `pipelines.90_anomaly_attribution` | Emit anomaly attribution tables for anomalous windows. | Perf, Anomaly, Io, Scoring, Config, Pyspark | Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Anomaly: libs/anomaly owns downstream anomaly attribution artifacts and attribution-vs-truth validation; Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Scoring: libs/scoring owns raw and calibrated anomaly scoring over fitted phase and structural artifacts | 198 | 1 | 0 | `pipeline_stage_90_anomaly_attribution` |
| 16 | 95 Emit Explorer Bundle | `pipelines.95_emit_explorer_bundle` | Emit a thin explorer-ready bundle for notebook and UI consumers. | Io, Perf, Plotting, Reporting, Config | Io: libs/io owns artifact schemas, row contracts, and persistence/bridge utilities; Perf: libs/perf owns operational instrumentation helpers: MLflow integration; wall-time logging; memory observability snapshots; stage-manifest generation; Plotting: Plotting library functionality.; Reporting: Modules grouped under libs.reporting | 147 | 1 | 0 | `pipeline_stage_95_emit_explorer_bundle` |
